# Requirements for Code Compiler & Executer Python Client
# Authors: Rares-Nicholas Popa & Adrian-Petru Enache
# Date: April 2025

# Core dependencies (included in Python standard library)
# No external dependencies required for basic functionality

# Optional dependencies for enhanced features:

# For encrypted file transfers (optional)
# cryptography>=3.4.8

# For compressed file transfers (optional) 
# Already included in standard library via gzip module

# For configuration file parsing (optional)
# pyyaml>=6.0

# For colored terminal output (optional)
# colorama>=0.4.4

# For progress bars (optional)
# tqdm>=4.62.0

# For async networking (optional)
# aiofiles>=0.7.0

# Development dependencies (for testing and development)

# Testing framework
# pytest>=6.2.0
# pytest-asyncio>=0.15.0
# pytest-cov>=2.12.0

# Code formatting and linting
# black>=21.0.0
# flake8>=3.9.0
# mypy>=0.910

# Documentation
# sphinx>=4.0.0
# sphinx-rtd-theme>=0.5.0

# Note: The Python client is designed to work with Python 3.7+ 
# and uses only standard library modules for maximum compatibility.
# All dependencies listed above are optional and only needed for
# enhanced features or development work.

# To install optional dependencies:
# pip install cryptography pyyaml colorama tqdm

# To install development dependencies:
# pip install pytest pytest-cov black flake8 mypy

# For a complete development environment:
# pip install -r requirements-dev.txt